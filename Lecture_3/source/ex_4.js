// Define an anonymous function that returns "Anonymous!" and assign it to a variable.

const xFunc = function () {
    return 'Anonymous';
}

const arrowFunc = () => 'Anonymous';

console.log(arrowFunc());
